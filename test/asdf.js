var content
//const package = require('../package.json');
const fs = require('fs');
var test = fs.readFile('../package.json', function(err, data) {
    content = data
    console.log(content);
    var info;
    parse.
    info=JSON.stringify(content);
    console.log(info);
    }

)